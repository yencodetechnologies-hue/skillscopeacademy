# Loaded automatically by `vite dev` (npm run dev).
# Points the dev frontend at the local backend so file edits are seen instantly.
VITE_API_URL=http://localhost:7001

# Square Web Payments (Sandbox) — public application id only
VITE_SQUARE_APPLICATION_ID=sandbox-sq0idb-_XYZR5_euF_OdIV6_OC0Sg
VITE_SQUARE_ENVIRONMENT=sandbox
